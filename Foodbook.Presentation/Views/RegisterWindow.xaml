<Window x:Class="Foodbook.Presentation.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🍽️ Foodbook - Sign Up" 
        Height="850" Width="1300"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- Gradient Background Brush -->
        <LinearGradientBrush x:Key="MainGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#667eea" Offset="0"/>
            <GradientStop Color="#764ba2" Offset="1"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="CardGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF6B6B" Offset="0"/>
            <GradientStop Color="#4ECDC4" Offset="1"/>
        </LinearGradientBrush>

        <!-- Modern Button with Glass Effect -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="24,16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="12" 
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="8" ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#059669"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#047857"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Enhanced TextBox with Glass Effect -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="4" ShadowDepth="1"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#10B981"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#6B7280"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#3B82F6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" 
                                   Foreground="{TemplateBinding Foreground}"
                                   FontSize="{TemplateBinding FontSize}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="TextDecorations" Value="Underline"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <!-- Animated Gradient Background -->
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#667eea" Offset="0"/>
                    <GradientStop Color="#764ba2" Offset="0.5"/>
                    <GradientStop Color="#f093fb" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
        </Grid>

        <!-- Floating Particles Effect -->
        <Canvas>
            <Ellipse Canvas.Left="120" Canvas.Top="180" Width="5" Height="5" Fill="White" Opacity="0.6"/>
            <Ellipse Canvas.Left="350" Canvas.Top="220" Width="7" Height="7" Fill="White" Opacity="0.4"/>
            <Ellipse Canvas.Left="600" Canvas.Top="120" Width="4" Height="4" Fill="White" Opacity="0.7"/>
            <Ellipse Canvas.Left="900" Canvas.Top="280" Width="6" Height="6" Fill="White" Opacity="0.5"/>
            <Ellipse Canvas.Left="1100" Canvas.Top="200" Width="5" Height="5" Fill="White" Opacity="0.6"/>
        </Canvas>
        
        <!-- Main Content -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Register Form with Glass Morphism -->
            <Border Grid.Column="0" 
                    Background="#FFFFFF" 
                    Margin="60,60,30,60" 
                    CornerRadius="20"
                    Opacity="0.95">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.15" BlurRadius="20" ShadowDepth="5"/>
                </Border.Effect>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="40">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Enhanced Logo/Brand -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="🍽️" FontSize="32" Margin="0,0,8,0"/>
                            <TextBlock Text="Foodbook" 
                                       FontSize="32" 
                                       FontWeight="Bold" 
                                       Foreground="#1F2937"/>
                        </StackPanel>
                        
                        <!-- Welcome Title with Gradient -->
                        <TextBlock Grid.Row="1" 
                                   Text="Create Your Account!" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Left"
                                   Margin="0,0,0,8">
                            <TextBlock.Foreground>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#10B981" Offset="0"/>
                                    <GradientStop Color="#3B82F6" Offset="1"/>
                                </LinearGradientBrush>
                            </TextBlock.Foreground>
                        </TextBlock>
                        
                        <!-- Enhanced Subtitle -->
                        <TextBlock Grid.Row="2" 
                                   Text="🌟 Join our community of food lovers and discover amazing recipes" 
                                   FontSize="15" 
                                   Foreground="#6B7280"
                                   HorizontalAlignment="Left"
                                   Margin="0,0,0,32"/>
                        
                        <!-- Enhanced Username Field -->
                        <StackPanel Grid.Row="3" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="👤" FontSize="16" Margin="0,0,6,0"/>
                                <TextBlock Text="Username" 
                                           FontSize="15" 
                                           FontWeight="SemiBold" 
                                           Foreground="#374151"/>
                            </StackPanel>
                            <TextBox x:Name="UsernameTextBox" 
                                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                     Style="{StaticResource ModernTextBox}"
                                     Height="56"/>
                        </StackPanel>
                        
                        <!-- Enhanced Email Field -->
                        <StackPanel Grid.Row="4" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="📧" FontSize="16" Margin="0,0,6,0"/>
                                <TextBlock Text="Email Address" 
                                           FontSize="15" 
                                           FontWeight="SemiBold" 
                                           Foreground="#374151"/>
                            </StackPanel>
                            <TextBox x:Name="EmailTextBox" 
                                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                     Style="{StaticResource ModernTextBox}"
                                     Height="56"/>
                        </StackPanel>
                        
                        <!-- Enhanced Password Field -->
                        <StackPanel Grid.Row="5" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="🔒" FontSize="16" Margin="0,0,6,0"/>
                                <TextBlock Text="Password" 
                                           FontSize="15" 
                                           FontWeight="SemiBold" 
                                           Foreground="#374151"/>
                            </StackPanel>
                            <PasswordBox x:Name="PasswordBox" 
                                         Height="56"
                                         Padding="16,14"
                                         FontSize="15"
                                         BorderThickness="2"
                                         BorderBrush="#E5E7EB"
                                         Background="White"
                                         VerticalContentAlignment="Center">
                                <PasswordBox.Style>
                                    <Style TargetType="PasswordBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="PasswordBox">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="10"
                                                            Padding="{TemplateBinding Padding}">
                                                        <Border.Effect>
                                                            <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="4" ShadowDepth="1"/>
                                                        </Border.Effect>
                                                        <ScrollViewer x:Name="PART_ContentHost" 
                                                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsFocused" Value="True">
                                                            <Setter Property="BorderBrush" Value="#10B981"/>
                                                            <Setter Property="BorderThickness" Value="2"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="BorderBrush" Value="#6B7280"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </PasswordBox.Style>
                            </PasswordBox>
                        </StackPanel>
                        
                        <!-- Enhanced Confirm Password Field -->
                        <StackPanel Grid.Row="6" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="🔐" FontSize="16" Margin="0,0,6,0"/>
                                <TextBlock Text="Confirm Password" 
                                           FontSize="15" 
                                           FontWeight="SemiBold" 
                                           Foreground="#374151"/>
                            </StackPanel>
                            <PasswordBox x:Name="ConfirmPasswordBox" 
                                         Height="56"
                                         Padding="16,14"
                                         FontSize="15"
                                         BorderThickness="2"
                                         BorderBrush="#E5E7EB"
                                         Background="White"
                                         VerticalContentAlignment="Center">
                                <PasswordBox.Style>
                                    <Style TargetType="PasswordBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="PasswordBox">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="10"
                                                            Padding="{TemplateBinding Padding}">
                                                        <Border.Effect>
                                                            <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="4" ShadowDepth="1"/>
                                                        </Border.Effect>
                                                        <ScrollViewer x:Name="PART_ContentHost" 
                                                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsFocused" Value="True">
                                                            <Setter Property="BorderBrush" Value="#10B981"/>
                                                            <Setter Property="BorderThickness" Value="2"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="BorderBrush" Value="#6B7280"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </PasswordBox.Style>
                            </PasswordBox>
                        </StackPanel>
                        
                        <!-- Enhanced Terms Agreement -->
                        <CheckBox Grid.Row="7" 
                                  Content="📋 I agree to the Terms of Service and Privacy Policy" 
                                  IsChecked="{Binding AgreeToTerms}"
                                  FontSize="15" 
                                  FontWeight="SemiBold"
                                  Foreground="#374151"
                                  Margin="0,0,0,24"/>
                        
                        <!-- Enhanced Sign Up Button -->
                        <Button Grid.Row="8" 
                                Content="🚀 Create Account" 
                                Style="{StaticResource ModernButton}"
                                Click="SignUp_Click"
                                Margin="0,0,0,24"/>
                        
                        <!-- Enhanced Login Link -->
                        <StackPanel Grid.Row="9" 
                                    Orientation="Horizontal" 
                                    HorizontalAlignment="Center">
                            <TextBlock Text="💡 Already have an account? " 
                                       FontSize="15" 
                                       Foreground="#6B7280"/>
                            <Button Content="✨ Sign In" 
                                    Style="{StaticResource LinkButton}"
                                    Click="SignIn_Click"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Border>
            
            <!-- Enhanced Food Image Background -->
            <Border Grid.Column="1" 
                    Margin="30,60,60,60" 
                    CornerRadius="20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#667eea" Offset="0"/>
                        <GradientStop Color="#764ba2" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="20" ShadowDepth="5"/>
                </Border.Effect>
                <Grid>
                    <!-- Animated Background Elements -->
                    <Canvas>
                        <Ellipse Canvas.Left="60" Canvas.Top="120" Width="9" Height="9" Fill="White" Opacity="0.3"/>
                        <Ellipse Canvas.Left="220" Canvas.Top="180" Width="7" Height="7" Fill="White" Opacity="0.5"/>
                        <Ellipse Canvas.Left="380" Canvas.Top="100" Width="11" Height="11" Fill="White" Opacity="0.4"/>
                        <Ellipse Canvas.Left="480" Canvas.Top="240" Width="8" Height="8" Fill="White" Opacity="0.6"/>
                    </Canvas>
                    
                    <!-- Enhanced Decorative food elements -->
                    <StackPanel HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                Margin="50">
                        <TextBlock Text="👨‍🍳" 
                                   FontSize="140" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,30"/>
                        <TextBlock Text="Join Our Community" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,12"/>
                        <TextBlock Text="🌟 Share your culinary journey" 
                                   FontSize="20" 
                                   Foreground="#E5E7EB"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,20"/>
                        <TextBlock Text="🍳 Create and discover amazing recipes" 
                                   FontSize="16" 
                                   Foreground="#D1D5DB"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Enhanced Close Button -->
        <Button Content="✕" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Margin="0,25,25,0"
                Width="40" 
                Height="40"
                Background="Transparent"
                Foreground="White"
                FontSize="18"
                FontWeight="Bold"
                BorderThickness="0"
                Click="Close_Click"
                Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="20">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="8" ShadowDepth="2"/>
                                    </Border.Effect>
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#EF4444"/>
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#DC2626"/>
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
