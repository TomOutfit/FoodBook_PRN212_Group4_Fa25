<UserControl x:Class="Foodbook.Presentation.Views.Tabs.IngredientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel>
        <Border Background="White" CornerRadius="20" Padding="32" Margin="0,0,0,24">
            <Border.Effect>
                <DropShadowEffect Color="#FF6B6B" Direction="270" ShadowDepth="8" Opacity="0.15" BlurRadius="20"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Border Background="#FF6B6B" CornerRadius="12" Padding="8" Margin="0,0,16,0" Height="57">
                            <TextBlock Text="🥘" FontSize="24" Foreground="White"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="Smart Inventory &amp; Meal Planner Hub" FontSize="36" FontWeight="Bold" Foreground="#1F2937"/>
                            <TextBlock Text="Reduce waste, optimize shopping, and discover cook-now recipes (AI)" FontSize="16" Foreground="#6B7280" Margin="0,4,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="#FFEBEE" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                            <StackPanel>
                                <TextBlock Text="Near Expiration" FontWeight="Bold"/>
                                <TextBlock Text="{Binding NearExpiryCount}" FontSize="22" FontWeight="Bold" Foreground="#EF4444"/>
                                <TextBlock Text="Within 7 days" Foreground="#6B7280"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Background="#ECFDF5" CornerRadius="12" Padding="16" Margin="6,0,6,0">
                            <StackPanel>
                                <TextBlock Text="Shopping Alerts" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ShoppingAlertsCount}" FontSize="22" FontWeight="Bold" Foreground="#10B981"/>
                                <TextBlock Text="At/Below Min Qty" Foreground="#6B7280"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="#EFF6FF" CornerRadius="12" Padding="16" Margin="12,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Total Items" FontWeight="Bold"/>
                                <TextBlock Text="{Binding TotalIngredients}" FontSize="22" FontWeight="Bold" Foreground="#3B82F6"/>
                                <TextBlock Text="In Stock" Foreground="#6B7280"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button Background="#FF6B6B" Foreground="White" FontSize="14" FontWeight="SemiBold" Padding="24,16" BorderThickness="0" Margin="0,0,12,0" Command="{Binding AddIngredientCommand}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="➕" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Add Ingredient"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <Button Background="#8B5CF6" Foreground="White" FontSize="14" FontWeight="SemiBold" Padding="24,16" BorderThickness="0" Command="{Binding ViewIngredientAnalyticsCommand}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Analytics"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Background="White" CornerRadius="20" Padding="32" Margin="0,0,0,24">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="4" Opacity="0.08" BlurRadius="16"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal">
                        <Border Padding="16,10" Margin="0,0,8,0" CornerRadius="20" Background="#FF6B6B" Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftClick" Command="{Binding NavigateToAllIngredientsCommand}"/>
                            </Border.InputBindings>
                            <TextBlock Text="{Binding Ingredients.Count, StringFormat={}All ({0})}" Foreground="White" FontWeight="SemiBold" FontSize="14"/>
                        </Border>
                        <Border Padding="16,10" Margin="0,0,8,0" CornerRadius="20" Background="#F1F5F9" Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftClick" Command="{Binding NavigateToProteinsCommand}"/>
                            </Border.InputBindings>
                            <TextBlock Text="🥩 Proteins" Foreground="#374151" FontWeight="Medium" FontSize="14"/>
                        </Border>
                        <Border Padding="16,10" Margin="0,0,8,0" CornerRadius="20" Background="#F1F5F9" Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftClick" Command="{Binding NavigateToGrainsCommand}"/>
                            </Border.InputBindings>
                            <TextBlock Text="🌾 Grains" Foreground="#374151" FontWeight="Medium" FontSize="14"/>
                        </Border>
                        <Border Padding="16,10" Margin="0,0,8,0" CornerRadius="20" Background="#F1F5F9" Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftClick" Command="{Binding NavigateToVegetablesCommand}"/>
                            </Border.InputBindings>
                            <TextBlock Text="🥕 Vegetables" Foreground="#374151" FontWeight="Medium" FontSize="14"/>
                        </Border>
                        <Border Padding="16,10" Margin="0,0,8,0" CornerRadius="20" Background="#F1F5F9" Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftClick" Command="{Binding NavigateToSpicesCommand}"/>
                            </Border.InputBindings>
                            <TextBlock Text="🧂 Spices" Foreground="#374151" FontWeight="Medium" FontSize="14"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
                <Border Grid.Column="1" Background="#F8FAFC" CornerRadius="12" Padding="16,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Sort by:" FontSize="14" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <ComboBox SelectedValue="{Binding IngredientSortBy, Mode=TwoWay}" SelectedValuePath="Content" Width="160" Height="34" FontSize="14" Background="White" BorderThickness="0">
                            <ComboBoxItem Content="📝 Name A-Z"/>
                            <ComboBoxItem Content="📝 Name Z-A"/>
                            <ComboBoxItem Content="📂 Category"/>
                            <ComboBoxItem Content="📅 Date Added"/>
                        </ComboBox>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Ingredient Cards Grid -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ItemsControl Grid.Row="0" ItemsSource="{Binding PagedIngredients}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Margin="0,0,0,16" ItemWidth="360" ItemHeight="Auto"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FFFFFF" BorderBrush="#10B981" BorderThickness="2" CornerRadius="16" Padding="0" Margin="4" MinWidth="340" MaxWidth="360">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="4" Opacity="0.08" BlurRadius="12"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="112"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon/Color panel (left) -->
                                <Border Grid.Column="0" CornerRadius="16,0,0,16" Background="#F8FAFC">
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock Text="🥗" FontSize="36"/>
                                    </StackPanel>
                                </Border>

                                <!-- Content (right) -->
                                <Border Grid.Column="1" Background="#FFFFFF" CornerRadius="0,16,16,0" Padding="12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                            <TextBlock Text="{Binding Name}" FontSize="17" FontWeight="Bold" Foreground="#1F2937" TextWrapping="Wrap" MaxWidth="200"/>
                                        </StackPanel>
                                        <WrapPanel Margin="0,0,0,8">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,8,4">
                                                <TextBlock Text="📦" Margin="0,0,4,0" FontSize="11"/>
                                                <TextBlock Text="{Binding Quantity, StringFormat={}{0}}" Foreground="#6B7280" FontSize="12"/>
                                                <TextBlock Text=" " FontSize="12"/>
                                                <TextBlock Text="{Binding Unit}" Foreground="#6B7280" FontSize="12"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,8,4">
                                                <TextBlock Text="📂" Margin="0,0,4,0" FontSize="11"/>
                                                <TextBlock Text="{Binding Category}" Foreground="#6B7280" FontSize="12"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,8,4">
                                                <TextBlock Text="⏳" Margin="0,0,4,0" FontSize="11"/>
                                                <TextBlock Text="{Binding ExpiryDate, StringFormat={}{0:yyyy-MM-dd}}" Foreground="#6B7280" FontSize="12"/>
                                            </StackPanel>
                                        </WrapPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Border Background="#ECFDF5" CornerRadius="8" Padding="8,6" BorderThickness="0" Margin="0,0,8,0">
                                                <TextBlock Text="In stock" Foreground="#10B981" FontWeight="SemiBold" FontSize="12"/>
                                            </Border>
                                            <Border Background="#FEF3C7" CornerRadius="8" Padding="8,6" BorderThickness="0" Margin="0,0,8,0">
                                                <TextBlock Text="{Binding Location, StringFormat={}Location: {0}}" Foreground="#92400E" FontWeight="SemiBold" FontSize="12"/>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                <Button Style="{StaticResource PaginationButton}" Content="◄" Command="{Binding PrevPageCommand}" Margin="4,0"/>
                <Border Background="White" CornerRadius="8" Padding="8,4" Margin="8,0" BorderBrush="{DynamicResource DividerBrush}" BorderThickness="1">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Page" Foreground="#6B7280" Margin="0,0,6,0"/>
                        <ComboBox Width="72" Height="28" ItemsSource="{Binding PageOptions}" SelectedItem="{Binding SelectedPage, Mode=TwoWay}" BorderThickness="0" Background="Transparent"/>
                        <TextBlock Text="/" Foreground="#6B7280" Margin="6,0"/>
                        <TextBlock Text="{Binding TotalPages}"/>
                    </StackPanel>
                </Border>
                <Button Style="{StaticResource PaginationButton}" Content="►" Command="{Binding NextPageCommand}" Margin="4,0"/>
            </StackPanel>
        </Grid>

        <!-- Category summary blocks -->
        <Grid Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>
    </StackPanel>
</UserControl>