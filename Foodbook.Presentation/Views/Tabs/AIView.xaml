<UserControl x:Class="Foodbook.Presentation.Views.Tabs.AIView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel>
        <TextBlock Text="🤖 AI Chef &amp; Judge" FontSize="24" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,20"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="🎯 AI Judge" FontSize="18" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,8"/>
                    <TextBlock Text="Upload a photo of your dish and get AI-powered feedback on presentation, technique, and suggestions for improvement." FontSize="14" Foreground="#6B7280" TextWrapping="Wrap" Margin="0,0,0,16" Height="60"/>
                    <Button Content="📸 Upload Photo &amp; Judge" Background="#8B5CF6" Foreground="White" FontSize="14" FontWeight="SemiBold" Padding="16,12" BorderThickness="0" HorizontalAlignment="Stretch" Command="{Binding JudgeDishCommand}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="20" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="👨‍🍳 AI Chef" FontSize="18" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,8"/>
                    <TextBlock Text="Let AI create custom recipes, analyze nutrition, and provide cooking guidance based on your preferences." FontSize="14" Foreground="#6B7280" TextWrapping="Wrap" Margin="0,0,0,16" Height="60"/>
                    <Button Content="🎲 Generate Recipe" Background="#4CAF50" Foreground="White" FontSize="14" FontWeight="SemiBold" Padding="16,12" BorderThickness="0" HorizontalAlignment="Stretch" Command="{Binding GenerateRecipeCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
        <Grid Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="🛒 Smart Shopping List" FontSize="18" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,8"/>
                    <TextBlock Text="AI analyzes your recipes and creates an optimized shopping list with quantities and store layout suggestions." FontSize="14" Foreground="#6B7280" TextWrapping="Wrap" Margin="0,0,0,16" Height="60"/>
                    <Button Content="📋 Generate Shopping List" Background="#F59E0B" Foreground="White" FontSize="14" FontWeight="SemiBold" Padding="16,12" BorderThickness="0" HorizontalAlignment="Stretch" Command="{Binding GenerateShoppingListCommand}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="20" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="📊 Nutrition Analysis" FontSize="18" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,8"/>
                    <TextBlock Text="Get detailed nutritional information and health recommendations for your recipes using AI analysis." FontSize="14" Foreground="#6B7280" TextWrapping="Wrap" Margin="0,0,0,16" Height="60"/>
                    <Button Content="🥗 Analyze Nutrition" Background="#E74C3C" Foreground="White" FontSize="14" FontWeight="SemiBold" Padding="16,12" BorderThickness="0" HorizontalAlignment="Stretch" Command="{Binding OpenNutritionAnalysisCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>


