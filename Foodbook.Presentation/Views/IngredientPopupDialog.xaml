<Window x:Class="Foodbook.Presentation.Views.IngredientPopupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ingredient Management" 
        Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8F9FA">
    
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="2" Opacity="0.1" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderBrush}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" 
                                            Grid.Column="2" 
                                            Focusable="false" 
                                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                            ClickMode="Press">
                                    <Path x:Name="Arrow" 
                                          Fill="Gray" 
                                          HorizontalAlignment="Right" 
                                          Margin="0,0,4,0" 
                                          VerticalAlignment="Center" 
                                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite" 
                                                IsHitTestVisible="False" 
                                                Content="{TemplateBinding SelectionBoxItem}" 
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                                Margin="3,3,23,3" 
                                                VerticalAlignment="Center" 
                                                HorizontalAlignment="Left"/>
                                <TextBox x:Name="PART_EditableTextBox" 
                                       IsReadOnly="True" 
                                       Style="{x:Null}" 
                                       HorizontalAlignment="Left" 
                                       VerticalAlignment="Center" 
                                       Margin="3,3,23,3" 
                                       Focusable="True" 
                                       Background="Transparent" 
                                       Visibility="Hidden"/>
                                <Popup x:Name="Popup" 
                                     Placement="Bottom" 
                                     IsOpen="{TemplateBinding IsDropDownOpen}" 
                                     AllowsTransparency="True" 
                                     Focusable="False" 
                                     PopupAnimation="Slide">
                                    <Grid x:Name="DropDown" 
                                        SnapsToDevicePixels="True" 
                                        MinWidth="{TemplateBinding ActualWidth}" 
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder" 
                                              Background="White" 
                                              BorderThickness="1" 
                                              BorderBrush="#E5E7EB" 
                                              CornerRadius="8">
                                            <ScrollViewer Margin="4,6,4,6" 
                                                        SnapsToDevicePixels="True">
                                                <StackPanel IsItemsHost="True" 
                                                          KeyboardNavigation.DirectionalNavigation="Contained"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="🥕 Ingredient Management" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Basic Information -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Ingredient Information" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Name -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,16">
                                <TextBlock Text="Ingredient Name *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding Name}" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>
                            
                            <!-- Category -->
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,16">
                                <TextBlock Text="Category" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ComboBox SelectedItem="{Binding Category}" 
                                         Padding="12" 
                                         BorderThickness="1" 
                                         BorderBrush="#E5E7EB" 
                                         Background="White" 
                                         FontSize="14" 
                                         Height="40">
                                    <ComboBoxItem Content="Protein" IsSelected="True"/>
                                    <ComboBoxItem Content="Vegetable"/>
                                    <ComboBoxItem Content="Fruit"/>
                                    <ComboBoxItem Content="Grain"/>
                                    <ComboBoxItem Content="Dairy"/>
                                    <ComboBoxItem Content="Fat"/>
                                    <ComboBoxItem Content="Seasoning"/>
                                    <ComboBoxItem Content="Nuts"/>
                                    <ComboBoxItem Content="Legume"/>
                                </ComboBox>
                            </StackPanel>
                            
                            <!-- Unit -->
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,16">
                                <TextBlock Text="Unit" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ComboBox SelectedItem="{Binding Unit}" 
                                         Padding="12" 
                                         BorderThickness="1" 
                                         BorderBrush="#E5E7EB" 
                                         Background="White" 
                                         FontSize="14" 
                                         Height="40">
                                    <ComboBoxItem Content="kg"/>
                                    <ComboBoxItem Content="g" IsSelected="True"/>
                                    <ComboBoxItem Content="liter"/>
                                    <ComboBoxItem Content="ml"/>
                                    <ComboBoxItem Content="cup"/>
                                    <ComboBoxItem Content="tbsp"/>
                                    <ComboBoxItem Content="tsp"/>
                                    <ComboBoxItem Content="piece"/>
                                    <ComboBoxItem Content="dozen"/>
                                </ComboBox>
                            </StackPanel>
                            
                            <!-- Quantity -->
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,16">
                                <TextBlock Text="Quantity in Stock" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding Quantity}" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>
                            
                            <!-- Nutrition Info -->
                            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,16">
                                <TextBlock Text="Nutrition Information" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding NutritionInfo}" 
                                         Style="{StaticResource ModernTextBox}" 
                                         Height="80" 
                                         TextWrapping="Wrap" 
                                         AcceptsReturn="True"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="❌ Cancel" 
                    Style="{StaticResource ModernButton}" 
                    Background="#6B7280" 
                    Click="CancelButton_Click"/>
            <Button Content="💾 Save Ingredient" 
                    Style="{StaticResource ModernButton}" 
                    Background="#4CAF50" 
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
