<Window x:Class="Foodbook.Presentation.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🍽️ Foodbook - Sign Up" 
        Height="750" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#059669"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#047857"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#3B82F6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" 
                                   Foreground="{TemplateBinding Foreground}"
                                   FontSize="{TemplateBinding FontSize}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="TextDecorations" Value="Underline"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <!-- Beautiful gradient background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#667eea" Offset="0"/>
                <GradientStop Color="#764ba2" Offset="0.5"/>
                <GradientStop Color="#f093fb" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <!-- Background image layer -->
        <Image Source="pack://application:,,,/Foodbook.Presentation;component/Images/Healthy.jpg"
               Stretch="UniformToFill"
               Opacity="0.9"/>

        <!-- Decorative overlay -->
        <Grid Opacity="0.1">
            <Ellipse Fill="White" Width="300" Height="300" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-150,-150,0,0"/>
            <Ellipse Fill="White" Width="200" Height="200" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-100,-100"/>
            <Ellipse Fill="White" Width="150" Height="150" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0"/>
        </Grid>
        
        <!-- Main Content -->
        <Grid>
            <!-- Centered Register Form Card -->
            <Border 
                    Background="White" 
                    Margin="260,40,260,40" 
                    CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" 
                                      BlurRadius="20" 
                                      ShadowDepth="0" 
                                      Opacity="0.18"/>
                </Border.Effect>
                <Grid Margin="40,30,40,30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Welcome Title -->
                    <TextBlock Grid.Row="0" 
                               Text="Create Account" 
                               FontSize="22" 
                               FontWeight="Bold" 
                               Foreground="#1F2937"
                               HorizontalAlignment="Left"
                               Margin="0,0,0,4"/>
                    
                    <!-- Subtitle -->
                    <TextBlock Grid.Row="1" 
                               Text="Join our community of food lovers and discover amazing recipes." 
                               FontSize="13" 
                               Foreground="#6B7280"
                               HorizontalAlignment="Left"
                               Margin="0,0,0,16"
                               TextWrapping="Wrap"/>
                    
                    <!-- Username Field with inline logo icon -->
                    <StackPanel Grid.Row="2" Margin="0,0,0,12">
                        <TextBlock Text="Username" 
                                   FontSize="13" 
                                   FontWeight="SemiBold" 
                                   Foreground="#374151"
                                   Margin="0,0,0,4"/>
                        <Grid Height="48">
                            <Image Source="pack://application:,,,/Foodbook.Presentation;component/Images/FoodBook_Logo.png"
                                   Width="18" Height="18" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            <TextBox x:Name="UsernameTextBox" 
                                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                     Style="{StaticResource ModernTextBox}"
                                     Padding="38,10,12,10"
                                     FontSize="13"/>
                        </Grid>
                    </StackPanel>
                    
                    <!-- Email Field with inline logo icon -->
                    <StackPanel Grid.Row="3" Margin="0,0,0,12">
                        <TextBlock Text="Email Address" 
                                   FontSize="13" 
                                   FontWeight="SemiBold" 
                                   Foreground="#374151"
                                   Margin="0,0,0,4"/>
                        <Grid Height="48">
                            <Image Source="pack://application:,,,/Foodbook.Presentation;component/Images/FoodBook_Logo.png"
                                   Width="18" Height="18" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            <TextBox x:Name="EmailTextBox" 
                                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                     Style="{StaticResource ModernTextBox}"
                                     Padding="38,10,12,10"
                                     FontSize="13"/>
                        </Grid>
                    </StackPanel>
                    
                    <!-- Password Field with inline logo icon -->
                    <StackPanel Grid.Row="4" Margin="0,0,0,12">
                        <TextBlock Text="Password" 
                                   FontSize="13" 
                                   FontWeight="SemiBold" 
                                   Foreground="#374151"
                                   Margin="0,0,0,4"/>
                        <Grid Height="48">
                            <Image Source="pack://application:,,,/Foodbook.Presentation;component/Images/FoodBook_Logo.png"
                                   Width="18" Height="18" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            <PasswordBox x:Name="PasswordBox" 
                                         Padding="38,10,12,10"
                                         FontSize="13"
                                         BorderThickness="1"
                                         BorderBrush="#D1D5DB"
                                         Background="White"
                                         VerticalContentAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                    
                    <!-- Confirm Password Field with inline logo icon -->
                    <StackPanel Grid.Row="5" Margin="0,0,0,12">
                        <TextBlock Text="Confirm Password" 
                                   FontSize="13" 
                                   FontWeight="SemiBold" 
                                   Foreground="#374151"
                                   Margin="0,0,0,4"/>
                        <Grid Height="48">
                            <Image Source="pack://application:,,,/Foodbook.Presentation;component/Images/FoodBook_Logo.png"
                                   Width="18" Height="18" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            <PasswordBox x:Name="ConfirmPasswordBox" 
                                         Padding="38,10,12,10"
                                         FontSize="13"
                                         BorderThickness="1"
                                         BorderBrush="#D1D5DB"
                                         Background="White"
                                         VerticalContentAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                    
                    <!-- Terms Agreement -->
                    <CheckBox Grid.Row="6" 
                              Content="I agree to the Terms of Service and Privacy Policy" 
                              IsChecked="{Binding AgreeToTerms}"
                              FontSize="13" 
                              Foreground="#374151"
                              Margin="0,4,0,12"/>
                    
                    <!-- Sign Up Button -->
                    <Button Grid.Row="7" 
                            Content="Create Account" 
                            Style="{StaticResource ModernButton}"
                            Click="SignUp_Click"
                            Padding="20,10"
                            FontSize="15"
                            Margin="0,0,0,12"/>
                    
                    <!-- Login Link -->
                    <StackPanel Grid.Row="8" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Center"
                                Margin="0,4,0,0">
                        <TextBlock Text="Already have an account? " 
                                   FontSize="13" 
                                   Foreground="#6B7280"/>
                        <Button Content="Sign In" 
                                Style="{StaticResource LinkButton}"
                                FontSize="13"
                                Click="SignIn_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            
        </Grid>
        
        <!-- Close Button -->
        <Button Content="✕" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Margin="0,20,20,0"
                Width="36" 
                Height="36"
                Background="Transparent"
                Foreground="White"
                FontSize="18"
                FontWeight="Bold"
                BorderThickness="0"
                Click="Close_Click"
                Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="18">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF6B6B"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <Button.Effect>
                <DropShadowEffect Color="#000000" 
                                  BlurRadius="8" 
                                  ShadowDepth="2" 
                                  Opacity="0.3"/>
            </Button.Effect>
        </Button>
    </Grid>
</Window>
