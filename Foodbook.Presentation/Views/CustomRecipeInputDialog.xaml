<Window x:Class="Foodbook.Presentation.Views.CustomRecipeInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Recipe Analysis" 
        Height="500" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8F9FA">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🤖 Custom Recipe Analysis" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="#2C3E50" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Enter any recipe text and let AI parse the ingredients for nutritional analysis" 
                       FontSize="12" 
                       Foreground="#7F8C8D" 
                       HorizontalAlignment="Center"/>
        </StackPanel>
        
        <!-- Input Area -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8" 
                BorderBrush="#E9ECEF" 
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" 
                           Text="Recipe Text:" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Foreground="#2C3E50" 
                           Margin="0,0,0,10"/>
                
                <TextBox Name="RecipeTextBox" 
                         Grid.Row="1" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto"
                         FontSize="12"
                         Padding="10"
                         Background="#FAFAFA"
                         BorderBrush="#DEE2E6"
                         BorderThickness="1"
                         TextChanged="OnTextChanged"
                         VerticalAlignment="Stretch"/>
                
                <StackPanel Grid.Row="2" 
                            Orientation="Horizontal" 
                            Margin="0,10,0,0">
                    <TextBlock Text="💡 " FontSize="12" Foreground="#6C757D"/>
                    <TextBlock Text="Example: 500g chicken breast, 2 onions, 3 tbsp olive oil, salt, pepper" 
                               FontSize="11" 
                               Foreground="#6C757D" 
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Name="CancelButton" 
                    Content="Cancel" 
                    Width="80" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#6C757D" 
                    Foreground="White" 
                    BorderThickness="0" 
                    Click="OnCancelClicked"/>
            <Button Name="AnalyzeButton" 
                    Content="🤖 Analyze with AI" 
                    Width="140" 
                    Height="35" 
                    Background="#17A2B8" 
                    Foreground="White" 
                    BorderThickness="0" 
                    IsEnabled="False"
                    Click="OnAnalyzeClicked"/>
        </StackPanel>
    </Grid>
</Window>